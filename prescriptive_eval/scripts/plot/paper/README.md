# Paper Figures

Canonical location for main-paper figure scripts.

Legacy entrypoints under `scripts/plot/` should remain as thin wrappers so that
`regenerate_all.sh` and existing commands keep working during refactors.

